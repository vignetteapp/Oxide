namespace Oxide.JavaScript
{
    public enum JSTypedArrayType
    {
        Int8Array,
        Int16Array,
        Int32Array,
        Uint8Array,
        Uint8ClampedArray,
        Uint32Array,
        Float32Array,
        Float64Array,
        ArrayBuffer,
        None,
    }
}
